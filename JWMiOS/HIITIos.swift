import SwiftUI
import UIKit

struct HIITIos: View {
    var body: some View {
        // Start building your HIITWorkout timer and workout builder here
        Text("HIIT Workout Timer & Builder")
            .font(.title)
            .padding()
    }
}

#Preview {
    LandscapeContentView(rootView: HIITIos())
}
